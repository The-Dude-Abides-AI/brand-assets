name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  ci:
    uses: The-Dude-Abides-AI/.github/.github/workflows/ci-reusable.yml@main
    with:
      opus-review: ${{ github.event_name == 'pull_request' }}
    secrets: inherit
