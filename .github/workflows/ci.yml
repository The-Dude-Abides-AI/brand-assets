# CI — calls the shared reusable workflow
name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  ci:
    uses: the-dude-abides-ai/.github/.github/workflows/ci-reusable.yml@main
    secrets: inherit
